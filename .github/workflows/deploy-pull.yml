name: Deploy Laravel + React/Vite
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy por SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || '22' }}
          script_stop: true
          script: |
            set -e  # Detener si hay errores
            
            # Cargar variables de entorno
            source ~/.bashrc 2>/dev/null || true
            source ~/.nvm/nvm.sh 2>/dev/null || true
            
            # Ir al directorio del proyecto
            cd ${{ secrets.PROJECT_PATH }}
            
            echo "🚀 Iniciando deploy..."
            echo "📍 Directorio actual: $(pwd)"
            
            # Verificar herramientas
            echo "🔍 Verificando herramientas..."
            node --version || echo "❌ Node.js no encontrado"
            npm --version || echo "❌ npm no encontrado"
            composer --version || echo "❌ Composer no encontrado"
            
            # Manejar cambios locales de forma más robusta
            echo "🔍 Verificando estado del repositorio..."
            if git status --porcelain 2>/dev/null | grep -q .; then
              echo "⚠️ Hay cambios sin commitear, haciendo stash..."
              git stash push --include-untracked -m "Auto-stash before deploy $(date)"
            else
              echo "✅ Repositorio limpio"
            fi
            
            # Git pull con manejo de errores
            echo "🔄 Actualizando código..."
            git fetch origin main
            git checkout main
            git pull origin main
            
            # Obtener archivos cambiados (con fallback)
            CHANGED_FILES=""
            if git rev-parse HEAD~1 >/dev/null 2>&1; then
              CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD 2>/dev/null || echo "")
            fi
            
            echo "📝 Archivos cambiados: ${CHANGED_FILES:-"(primer deploy o error detectando cambios)"}"
            
            # Composer - verificar cambios o forzar si no se puede detectar
            if echo "$CHANGED_FILES" | grep -q "composer\.\(json\|lock\)" || [ -z "$CHANGED_FILES" ]; then
              echo "📦 Actualizando dependencias de Composer..."
              export COMPOSER_ALLOW_SUPERUSER=1
              composer install --no-dev --optimize-autoloader --no-interaction
            else
              echo "✅ Sin cambios en composer.json/lock"
            fi
            
            # NPM - verificar cambios o forzar si no se puede detectar
            if echo "$CHANGED_FILES" | grep -q "package\(-lock\)\?\.json" || [ -z "$CHANGED_FILES" ]; then
              echo "📦 Actualizando dependencias de npm..."
              if [ -f package-lock.json ]; then
                npm ci
              else
                npm install
              fi
            else
              echo "✅ Sin cambios en package.json/lock"
            fi
            
            # Migraciones - con mejor manejo de errores
            echo "🗄️ Verificando migraciones..."
            MIGRATE_STATUS=$(php artisan migrate:status --no-interaction 2>/dev/null || echo "ERROR")
            
            if [ "$MIGRATE_STATUS" != "ERROR" ] && echo "$MIGRATE_STATUS" | grep -q "No"; then
              echo "🗄️ Ejecutando migraciones pendientes..."
              php artisan migrate --force --no-interaction
            else
              echo "✅ No hay migraciones pendientes"
            fi
            
            # Build assets
            echo "🏗️ Construyendo assets..."
            npm run build
            
            # Limpieza de cache
            echo "🧹 Limpiando cache..."
            php artisan config:clear || echo "⚠️ Warning: config:clear falló"
            php artisan route:clear || echo "⚠️ Warning: route:clear falló"
            php artisan view:clear || echo "⚠️ Warning: view:clear falló"
            
            # Optimización
            echo "⚡ Optimizando para producción..."
            php artisan config:cache || echo "⚠️ Warning: config:cache falló"
            php artisan route:cache || echo "⚠️ Warning: route:cache falló"
            php artisan view:cache || echo "⚠️ Warning: view:cache falló"
            
            # Storage link
            php artisan storage:link 2>/dev/null || echo "⚠️ storage:link ya existe o falló"
            
            # Permisos
            echo "🔐 Estableciendo permisos..."
            mkdir -p storage/framework/{cache,sessions,views} bootstrap/cache 2>/dev/null || true
            chmod -R 775 storage bootstrap/cache 2>/dev/null || echo "⚠️ Warning: chmod falló"
            
            # Opcional: Recargar PHP-FPM (solo si tienes permisos)
            echo "🔁 Intentando recargar PHP-FPM..."
            sudo systemctl reload php8.3-fpm 2>/dev/null || \
            sudo systemctl reload php8.2-fpm 2>/dev/null || \
            sudo systemctl reload php-fpm 2>/dev/null || \
            echo "⚠️ No se pudo recargar PHP-FPM (normal si no tienes permisos sudo)"
            
            echo "✅ Deploy completado exitosamente!"