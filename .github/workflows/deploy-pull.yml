name: Deploy Laravel + React/Vite (Safe)
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy por SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || '22' }}
          script_stop: true
          script: |
            set -e  # Detener si hay errores
            
            # Cargar variables de entorno
            source ~/.bashrc 2>/dev/null || true
            source ~/.nvm/nvm.sh 2>/dev/null || true
            
            # Ir al directorio del proyecto
            cd ${{ secrets.PROJECT_PATH }}
            
            echo "🚀 Iniciando deploy..."
            echo "📍 Directorio actual: $(pwd)"
            
            # Verificar herramientas
            echo "🔍 Verificando herramientas..."
            node --version || echo "❌ Node.js no encontrado"
            npm --version || echo "❌ npm no encontrado" 
            composer --version || echo "❌ Composer no encontrado"
            
            # EVITAR git status completamente - hacer stash preventivo
            echo "🔄 Haciendo stash preventivo (por si acaso)..."
            git stash push --include-untracked -m "Auto-stash before deploy $(date)" 2>/dev/null || echo "✅ No había cambios para stash"
            
            # Git pull directo
            echo "🔄 Actualizando código..."
            git fetch origin main
            git checkout main 2>/dev/null || echo "Ya estamos en main"
            git pull origin main
            
            # Siempre ejecutar composer y npm (más seguro)
            echo "📦 Actualizando dependencias de Composer..."
            export COMPOSER_ALLOW_SUPERUSER=1
            composer install --no-dev --optimize-autoloader --no-interaction
            
            echo "📦 Actualizando dependencias de npm..."
            if [ -f package-lock.json ]; then
              npm ci
            else
              npm install
            fi
            
            # Migraciones - método más simple
            echo "🗄️ Ejecutando migraciones..."
            php artisan migrate --force --no-interaction 2>/dev/null || echo "✅ No hay migraciones o ya están ejecutadas"
            
            # Build assets
            echo "🏗️ Construyendo assets..."
            npm run build
            
            # Limpieza y optimización (sin fallar si hay errores)
            echo "🧹 Limpiando y optimizando..."
            php artisan config:clear 2>/dev/null || true
            php artisan route:clear 2>/dev/null || true
            php artisan view:clear 2>/dev/null || true
            php artisan config:cache 2>/dev/null || true
            php artisan route:cache 2>/dev/null || true
            php artisan view:cache 2>/dev/null || true
            php artisan storage:link 2>/dev/null || true
            
            # Permisos
            echo "🔐 Estableciendo permisos..."
            mkdir -p storage/framework/{cache,sessions,views} bootstrap/cache 2>/dev/null || true
            chmod -R 775 storage bootstrap/cache 2>/dev/null || true
            
            # Opcional: Recargar PHP-FPM
            echo "🔁 Recargando PHP-FPM..."
            sudo systemctl reload php8.3-fpm 2>/dev/null || \
            sudo systemctl reload php8.2-fpm 2>/dev/null || \
            sudo systemctl reload php-fpm 2>/dev/null || \
            echo "⚠️ No se pudo recargar PHP-FPM"
            
            echo "✅ Deploy completado exitosamente!"